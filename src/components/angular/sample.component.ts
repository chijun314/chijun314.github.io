import { NgIf } from '@angular/common';
import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-sample',
  standalone: true,
  template: `
    <p>I'm a sample!!</p>
  `,
})
export class SampleComponent {}